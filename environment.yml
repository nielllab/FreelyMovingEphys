# install: conda env create -f environment.yml
# update:  conda env update -f environment.yml

# for linux, replace
#   - "deeplabcut[gui]"
# with
#   - deeplabcut

name: ephys1
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - pip
  - jupyter
  - nb_conda
  - ffmpeg
  - matplotlib
  - scikit-image
  - xarray
  - tqdm
  - pytables
  - umap-learn
  - pip:
    - deeplabcut==2.2.0.6
    - astropy
    - wavio
    - pysimplegui
    - netcdf4
    - opencv-python
    - scipy
    - ray